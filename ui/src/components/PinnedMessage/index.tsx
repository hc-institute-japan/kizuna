import React from "react";
import styles from "./style.module.css";

const PinnedMessage: React.FC = () => {
  return <div className={`${styles["pinned-message"]} ion-padding`}></div>;
};

export default PinnedMessage;
